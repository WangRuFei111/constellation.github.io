<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightyellow</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="main_rb">
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:button">@null</item>
        <item name="android:textSize">12sp</item>
        <item name="android:drawablePadding">2dp</item>
        <item name="android:textColor">@drawable/main_rbtext</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_height">match_parent</item>
    </style>


    <!--    全屏显示，无状态栏的方法-->
    <style name="TranslucentTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>


    <!--    全屏显示,又透明状态栏得方法-->
    <style name="statusBar" parent="AppTheme">

    </style>

    <style name="statusBar.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="statusBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="statusBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
